<?php

namespace App\Installer;

// 70.php
use Illuminate\Console\OutputStyle;

class CreateSchedulers implements InstallStep{
    public function execute(OutputStyle $output, InstallConfig $config): int
    {
        // Turns scheduler

        // Kabal scheduler

        // Ibank scheduler

        // News scheduler

        // Planets scheduler

        // Ports scheduler

        // Tow scheduler

        // Ranking scheduler

        // Degrade scheduler

        // Apocalypse scheduler

        // Governor scheduler

        return 0;
    }
}